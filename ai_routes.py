from fastapi import APIRouter

# Módulo de AI desativado para a versão de produção do FastAPI Platform Lab.
# Este router existe apenas para compatibilidade, mas não registra nenhuma rota.

router = APIRouter()